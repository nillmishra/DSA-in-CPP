#include<iostream>
using namespace std;
int main()
{
    /*
    int n;
    cin >> n;

    cout << "value of n is :" << n <<endl;
    if (n >0)
    cout << "+ve number" << endl;
    else
    cout << "-ve number";
    */

   /*
   int a, b;
   cout << "Enter the value of a : ";
   cin >> a;
   cout << "Enter the value of b : ";
   cin >> b;
   cout << "value of a & b is " << a << " & " << b << endl;

   if (a > b)
   {
    cout << a <<" is greater than " << b << endl;
   }
   else{
    cout << b <<" is greater than " << a << endl;
   }
   */

  int a;
  cout<< "Enter the value iof a : ";
  cin >> a;
  if (a> 0)
  cout << "Ais positive";
  else if (a == 0)
  cout << "A is equal";
  else 
  cout << "A is negative";
  
    
}